<Window x:Class="BookStitch.Dialog.BoxedDiscWaitDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:dialog="clr-namespace:BookStitch.Dialog"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="CD einlegen"
        Icon="/Assets/Icons/BookStitchAppIcon-Simplified-multisize.ico"
        Width="640"
        Height="330"
        MinWidth="540"
        MinHeight="300"
        MaxWidth="780"
        MaxHeight="540"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource MainTextBrush}"
        FontFamily="Segoe UI"
        ShowInTaskbar="False"
        KeyboardNavigation.TabNavigation="Cycle">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="44" ResizeBorderThickness="7" CornerRadius="8" GlassFrameThickness="0"/>
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Style x:Key="DialogSecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
                <Setter Property="Background" Value="{StaticResource DisabledButtonBackgroundBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource MainTextBrush}"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#22303A"/>
                        <Setter Property="BorderBrush" Value="{StaticResource AccentSoftBrush}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <Border Background="{StaticResource AppBackgroundBrush}" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="{StaticResource PanelBackgroundBrush}" CornerRadius="8,8,0,0" BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="0,0,0,1" dialog:DialogWindowBehavior.IsDragRegion="True">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" Source="/Assets/Icons/BookStitchLogo-Round.png" Width="28" Height="28" Margin="12,0,10,0" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                    <TextBlock x:Name="TitleTextBlock" Grid.Column="1" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource AccentSoftBrush}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                    <TextBlock x:Name="DriveSeparatorTextBlock" Grid.Column="2" Margin="12,0,10,0" FontFamily="Segoe UI Symbol" FontSize="18" Foreground="{StaticResource AccentSoftBrush}" VerticalAlignment="Center" Text="&#x1F5B8;" Visibility="Collapsed"/>
                    <TextBlock x:Name="DriveTextBlock" Grid.Column="3" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource AccentSoftBrush}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Visibility="Collapsed"/>
                    <Button Grid.Column="4" Content="×" Margin="0,0,7,0" Style="{StaticResource DialogCloseButtonStyle}" shell:WindowChrome.IsHitTestVisibleInChrome="True" Click="CloseButton_Click"/>
                </Grid>
            </Border>
            <Grid Grid.Row="1" Margin="22,20,22,16">
                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <Border Grid.Column="0" Width="46" Height="46" CornerRadius="23" BorderThickness="1" BorderBrush="{StaticResource AccentSoftBrush}" Background="#14252A" Margin="0,0,16,0" VerticalAlignment="Top">
                    <TextBlock Text="↻" FontSize="25" FontWeight="SemiBold" Foreground="{StaticResource AccentSoftBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                        <TextBlock x:Name="HeadingTextBlock" FontSize="19" FontWeight="SemiBold" Foreground="{StaticResource MainTextBrush}" TextWrapping="Wrap"/>
                        <TextBlock x:Name="StatusTextBlock" Margin="0,10,0,0" Foreground="{StaticResource MainTextBrush}" TextWrapping="Wrap"/>
                        <Border Margin="0,14,0,0" Padding="12" Background="{StaticResource InputBackgroundBrush}" BorderBrush="{StaticResource InputBorderBrush}" BorderThickness="1" CornerRadius="6">
                            <TextBlock x:Name="HintTextBlock" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap"/>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
            <Border Grid.Row="2" Background="{StaticResource PanelBackgroundBrush}" BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="0,1,0,0" CornerRadius="0,0,8,8" Padding="14">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="CheckButton" Content="Fortfahren" Width="120" Height="34" Margin="0,0,8,0" Style="{StaticResource PrimaryButtonStyle}" Click="CheckButton_Click"/>
                    <Button Content="Unterbrechen" Width="150" Height="34" Style="{StaticResource DialogSecondaryButtonStyle}" Click="DeferButton_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
